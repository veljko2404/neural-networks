This repository contains a lightweight, educational framework for building neural networks entirely from scratch — without TensorFlow, PyTorch, or any automatic differentiation. Every gradient, every layer, every update step is implemented manually
